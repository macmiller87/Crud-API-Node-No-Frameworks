generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users {
  user_id   String   @id
  username  String
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
}
